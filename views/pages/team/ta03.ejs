<!DOCTYPE html>
<html>
<head>
  <% include ../includes/header.ejs %>
</head>

<body>
  <% include ../includes/nav.ejs %>
<div class="jumbotron text-center">
  <div class="container">
    <h1><%= title %></h1>
    <p>Team Activity 03</p>
  </div>
</div>
<div class="container">
  <form action="/team/ta03" method="POST" class='form-inline'>
  <input type="text" name="search" placeholder="Title Search" class="form-control" style="width:300px">
  <button type="submit" class="btn btn-default">Search</button>
  <% if (search.length > 0) { %>
    <a type="button" href="/team/ta03" class="btn btn-default">Clear Search</a>
    <h3>Showing results for <%= search %></h3>
    <% } %>
  </form>
  <h1>Results</h1>
  <% for (var i=0; i<data.length; i++) { %>
    <% if(i % 3 == 0) { %>
<% } %>
<div class="container">
  <div class="card">
    <img src="<% data[i].imageUrl %>" alt='t' class="carding">
    <div class="cardContainer">
      <h4><b><%= data.[i].name %></b></h4>
      <h6><%= data[i].price %></h6>
      <p><%= data[i].description %></p>
    </div>
    <% if (i % 3 == 2) { %>
    </div>
    <% } %>
  </div>
  <% } %>
</div>
</body>
</html>
